# Port targets — `supports-color-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `supports-color` that codex-rs uses, and therefore the minimum subset that `supports-color-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-144] (rank `173`, tier `direct`, status `To Do`)

**Cargo package name**: `supports-color`
**Rust module name (in `use` statements)**: `supports_color`

## Summary

- **1** distinct symbols imported by codex-rs
- **4** total `use` statements
- **4** codex source files affected
- **4** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `supports_color::Stream`

- **4 `use` site(s)** across **4 codex member crate(s)**: `codex-cli`, `codex-cloud-tasks`, `codex-exec`, `codex-tui`
- Aliases used: `as SupportStream`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `cli/src/main.rs:40` (`codex-cli`) — `use supports_color::Stream`
- `cloud-tasks/src/lib.rs:25` (`codex-cloud-tasks`) — `use supports_color::Stream as SupportStream`
- `exec/src/lib.rs:141` (`codex-exec`) — `use supports_color::Stream`
- `tui/src/main.rs:11` (`codex-tui`) — `use supports_color::Stream`
